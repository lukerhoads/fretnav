export type GeneralScale = {
  name: string;
  notes: [number, number, number, number, number, number, number];
};

export type Scale = {
  name: string;
  notes: [string, string, string, string, string, string, string];
};
